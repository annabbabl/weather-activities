# Navigate to the server directory, install/update dependencies, and start the server-side application in the background
cd server
npm i
npm start &

# Navigate to the client directory, install/update dependencies, and start the client-side application
cd ../client
npm i
npm start