{
  "name": "weather-activities",
  "version": "1.0.0",
  "description": "this is a website to post suggestions, what you can do on this day. This is a project for the \"Webentwicklungsmodul\"",
  "main": ".server/index.ts",
  "scripts": {
    "start": "node ./server/index.js",
    "dev": "ts-node server/index.ts",
    "build": "tsc && cd client && npm install && npm run build",
    "heroku-postbuild": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon --watch 'server/**/*.ts' --exec 'ts-node' server/index.ts"
  },
  "keywords": [
    "heroku",
    "weather"
  ],
  "author": "Anna Stabel",
  "license": "ISC",
  "dependencies": {
    "@types/cors": "^2.8.17",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.3",
    "express": "^4.18.2",
    "firebase": "^10.8.0",
    "firebase-admin": "^12.0.0",
    "firebase-auth": "^0.1.2",
    "firebase-functions": "^4.7.0",
    "multer": "^1.4.5-lts.1",
    "ts-node": "^10.9.2",
    "typescript": "^4.5.2"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.6",
    "@types/express": "^4.17.21",
    "@types/multer": "^1.4.11",
    "@types/node": "^16.0.0",
    "nodemon": "^3.0.3"
  },
  "engines": {
    "node": "^18.0.0"
  },
  "typescript": {
    "version": "^4.5.2"
  }
}
